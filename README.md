Программа на вход получает имя txt файла в виде строки и возвращает сначала самую короткую, потом самую длинную строку в файле в виде строки следующего вида:
Min = '{min_str}', max = '{max_str}'.
Везёт лошадка дровеньки,
На дровнях мужичок.
Срубил он нашу ёлочку
Под самый корешок.

Тогда функция должна вернуть: "Min = 'Под самый корешок.', max = 'Везёт лошадка дровеньки,'."
